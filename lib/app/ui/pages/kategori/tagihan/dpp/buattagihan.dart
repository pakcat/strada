import 'package:flutter/material.dart';
import 'package:get/get.dart';

class DPPPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('DPPPage')),
      body: Container(),
    );
  }
}
