import 'package:flutter/material.dart';
import 'package:get/get.dart';

class SPPPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('SPPPage')),
      body: Container(),
    );
  }
}
